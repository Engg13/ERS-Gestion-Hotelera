
  # Hotel booking front end

  This is a code bundle for Hotel booking front end. The original project is available at https://www.figma.com/design/FUsOQ22RAB1pwTdiVmSgTO/Hotel-booking-front-end.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  